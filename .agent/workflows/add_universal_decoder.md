---
description: Add Universal Decoder feature to the application
---

1. Create `src/utils/decoder.js` with analysis logic (patterns, math, gematria, anagrams).
2. Create `src/components/decoder/UniversalDecoder.jsx` with tabbed UI.
3. Update `src/App.jsx` to include the route `/decoder`.
4. Update `src/index.css` or component styles for specific animations/layouts if needed.
5. Create/Update unit tests if applicable.
6. Verify build with `npm run build`.
